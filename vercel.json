{
  "buildCommand": "curl -fsSL https://github.com/syao13/GeneSnap/releases/download/db-v1/genesnap.db -o api/genesnap.db && cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.py": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.py" },
    { "source": "/:path*",     "destination": "/index.html" }
  ]
}
