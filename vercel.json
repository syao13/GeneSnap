{
  "buildCommand": "pip install -r api/requirements.txt && PYTHONPATH=./backend/src DB_PATH=./api/genesnap.db python -m genesnap.db.seed && cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.py": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.py" },
    { "source": "/:path*",     "destination": "/index.html" }
  ]
}
