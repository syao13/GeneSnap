{
  "buildCommand": "pip install aiosqlite pydantic pydantic-settings && PYTHONPATH=./backend/src DB_PATH=./api/genesnap.db python -m genesnap.db.seed && cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.py" },
    { "source": "/:path*",     "destination": "/index.html" }
  ]
}
